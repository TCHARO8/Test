{% extends 'base.html.twig' %}

{% block title %}Service{% endblock %}

{% block body %}
    <h1>Service</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ source.id }}</td>
        </tr>
        <tr>
            <th>Code</th>
            <td>{{ source.code }}</td>
        </tr>
        <tr>
            <th>Libellé</th>
            <td>{{ source.libelleSource }}</td>
        </tr>
        <tr>
            <th>Département</th>
            <td>{{ source.departement.libelleD }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('app_service_index') }}">back to list</a>

    <a href="{{ path('app_source_edit', {'id': source.id}) }}">edit</a>

    {{ include('source/_delete_form.html.twig') }}
{% endblock %}
